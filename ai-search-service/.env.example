# Not used by default — the app runs entirely on local Ollama (zero API cost).
# Only needed if you intentionally switch the search endpoint to a hosted LLM.
ANTHROPIC_API_KEY=your-api-key-here

# Demo admin account — override before deploying anywhere beyond local/demo use.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
